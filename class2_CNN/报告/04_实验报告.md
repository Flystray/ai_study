# 第三部分　实验报告

## 1. 实验环境

| 项目 | 配置 |
|---|---|
| 操作系统 | Windows 10 Enterprise（64 位） |
| 编程语言 | Python 3.13（Anaconda） |
| 深度学习框架 | PyTorch 2.13.0（CPU 版）、torchvision 0.28.0 |
| 计算设备 | CPU（无 GPU），多核并行 |
| 其他依赖 | NumPy、Matplotlib、PIL |
| 代码仓库 | `code/` 目录，公共框架 `common.py`，各实验 `exp1~exp8.py` |

## 2. 实验总体设计

### 2.1 数据集

| 数据集 | 内容 | 尺寸 | 训练/测试 | 用途 |
|---|---|---|---|---|
| MNIST-digits | 手写数字 0-9 | 28×28 灰度 | 60k / 10k | 分类、蒸馏、AE、GAN |
| MNIST-Fashion | 10 类服饰 | 28×28 灰度 | 60k / 10k | 分类、蒸馏、AE |
| CIFAR-10 | 10 类自然图像 | 32×32 彩色 | 50k / 10k | 多通道 LeNet-5 |
| ImageNet（用户自备） | 1000 类 | 224×224 | — | 预训练模型验证 |

### 2.2 统一口径
- **固定随机种子**：全部实验 `set_seed(42)`，保证可复现；
- **优化器**：SGD（momentum=0.9）用于分类/蒸馏，Adam（lr=2e-4）用于生成式模型；
- **评估指标**：分类用测试集准确率（top-1），AE 用重建 MSE，GAN 用生成图像质量（可视化 + D/G loss）；
- **公平性设计**：LeNet-5 与 MLP 采用相同的 epoch、优化器与学习率，仅结构不同。

## 3. 实验一：LeNet-5 与全连接网络的对比

### 3.1 设计目的
验证 CNN 相对全连接网络在图像任务上的两个核心优势：
1. **精度更高**：卷积的局部连接与权值共享更契合图像空间结构；
2. **参数效率更高**：以更少参数取得更好效果。

### 3.2 模型与超参

| 模型 | 结构 | 参数量(MNIST) | 参数量(CIFAR-10) |
|---|---|---|---|
| LeNet-5 | C(6,5×5)→P→C(16,5×5)→P→FC(120,84,10)，ReLU | 44,426 | 62,006 |
| MLP | 784→200→100→10（CIFAR 用 512→256→10） | 178,110 | 1,707,274 |

训练配置：SGD(lr=1e-2, momentum=0.9)；MNIST/Fashion 各 8 epochs，CIFAR-10 各 15 epochs。

### 3.3 实验结果

> 训练至收敛（epoch 数：MNIST 8、Fashion 15、CIFAR-10 20，SGD lr=1e-2），曲线末端已趋于平稳。

**MNIST-digits（8 epochs）：**

| 模型 | 参数量 | 测试准确率 |
|---|---|---|
| LeNet-5 | 44,426 | **0.9879（98.79%）** |
| MLP | 178,110 | 0.9723（97.23%） |

![MNIST 训练曲线](../results/exp1_mnist_curve.png)

**MNIST-Fashion（15 epochs，已收敛）：**

| 模型 | 参数量 | 测试准确率 |
|---|---|---|
| LeNet-5 | 44,426 | **0.8925（89.25%）** |
| MLP | 178,110 | 0.8785（87.85%） |

![Fashion 训练曲线](../results/exp1_fashion_curve.png)

**CIFAR-10（20 epochs，多通道输入，已收敛）：**

| 模型 | 参数量 | 测试准确率 |
|---|---|---|
| LeNet-5 | 62,006 | **0.6643（66.43%）** |
| MLP | 1,707,274 | 0.4912（49.12%） |

![CIFAR-10 训练曲线](../results/exp1_cifar10_curve.png)

### 3.4 分析与结论

1. **三个数据集上 LeNet-5 全面优于 MLP**：MNIST +1.56pp、Fashion +1.40pp、CIFAR-10 **+17.31pp**。数据集越复杂、空间结构越重要，CNN 的优势越明显；
2. **参数效率**：CIFAR-10 上 MLP 参数量是 LeNet-5 的 **27.5 倍**，精度却低 17 个百分点。卷积的局部连接 + 权重共享使 CNN 用更少参数提取了更有用的特征；
3. **CIFAR-10 上的巨大差距**（66.4% vs 49.1%）说明：自然图像的空间局部性（相邻像素强相关）对全连接网络是"灾难性"的——MLP 逐像素展开破坏了空间结构，且 170 万参数在 5 万样本上严重过拟合、泛化差；而 CNN 的卷积归纳偏置天然适配图像。增大 epoch 后 MLP 反而从 50.4% 略降到 49.1%，是过拟合的直接表现；
4. **训练充分性**：初版 Fashion（8 epoch）与 CIFAR（15 epoch）曲线末端仍在上升，未充分收敛；将 epoch 增至 15 / 20 后，两者最终精度分别提升 0.57pp / 1.82pp，曲线末端趋于平稳，结论不变——CNN 的优势在收敛后更加稳固；
5. MLP 在 MNIST 上也达到 97%+，因为手写数字是"低方差"的简单任务，结构先验的重要性降低——**CNN 的价值随任务复杂度上升而放大**。

## 4. 实验二：LeNet-5 超参数对比

### 4.1 设计
在 MNIST 上以"单变量"原则对比 6 个超参数：卷积核大小（3/5/7）、池化种类（max/avg）、池化大小（2/3）、通道个数（6,16 / 12,32）、激活函数（relu/tanh/sigmoid）、stride（1/2）。为隔离单一变量影响，核大小对比时用 `padding=k//2` 保持特征图尺寸一致；每配置训练 4 epochs。

### 4.2 结果汇总（MNIST，各 4 epochs）

| 超参数 | 取值 | 测试准确率 |
|---|---|---|
| 卷积核大小 | 3×3 | 0.9823 |
| 卷积核大小 | 5×5（基线） | 0.9854 |
| 卷积核大小 | 7×7 | **0.9882** |
| 池化种类 | max | **0.9858** |
| 池化种类 | avg | 0.9812 |
| 池化大小 | 2×2 | **0.9858** |
| 池化大小 | 3×3 | 0.9845 |
| 通道个数 | (6,16) | 0.9855 |
| 通道个数 | (12,32) | **0.9905** |
| 激活函数 | relu | **0.9867** |
| 激活函数 | tanh | 0.9831 |
| 激活函数 | sigmoid | 0.1135（几乎不收敛） |
| stride | 1 | **0.9857** |
| stride | 2 | 0.9768 |

![超参数对比曲线](../results/exp2_hyperparam_curves.png)

### 4.3 分析与结论

1. **卷积核大小：越大越好（7×7 > 5×5 > 3×3）**。更大的感受野在 MNIST 上能捕获更完整的笔画结构；但 7×7 参数量更大、计算更贵，实际中 3×3 堆叠（VGG）是更优的折中；
2. **Max Pooling 优于 Average Pooling（+0.46pp）**：max 池化保留局部最强响应，利于特征边缘/纹理提取，这也是主流的默认选择；
3. **池化窗口 2×2 略优于 3×3**：更大的池化窗口过度下采样，丢失细节；
4. **通道数翻倍（6,16→12,32）提升显著（+0.5pp）**：更多滤波器＝更多特征维度，但代价是参数与计算量增长，需在容量与效率间权衡；
5. **激活函数差异巨大**：ReLU > tanh ≫ sigmoid。**sigmoid 几乎不收敛（11.35%）**，原因是其输出饱和区导数趋近 0，在多层网络中梯度逐层衰减（梯度消失），深层权重几乎不更新——这是深度学习论文普遍弃用 sigmoid 改用 ReLU 的直接实验证据；
6. **stride=2 降低精度（-0.89pp）**：stride 增大导致特征图快速缩小，信息在卷积早期被过多丢弃。

## 5. 实验三：知识蒸馏

### 5.1 设计
教师网络为 LeNet-5（从实验一直接复用），学生网络为更小的 MLP(128→64)。对比学生网络三种情况：
1. 独立训练（只用硬标签）；
2. 蒸馏训练（教师软标签 + 硬标签，温度 T=4，权重 α=0.5）。

> 蒸馏损失：$\mathcal{L}=\alpha\cdot\mathrm{CE}(q_s,y)+(1-\alpha)\cdot T^2\cdot D_{KL}(q_t^T\parallel q_s^T)$

### 5.2 实验结果

> 教师 LeNet-5 用**完整训练集**训练；学生 MLP(32) 只使用 **10% 训练数据**。
> 经超参扫描（`exp3_tune.py`）确定：温度越低、硬标签权重越高越优，最终采用 **T=1, alpha=0.7**。

**MNIST-digits（学生训练数据 6000 张）：**

| 模型 | 参数量 | 训练数据量 | 测试准确率 |
|---|---|---|---|
| 教师 LeNet-5 | 44,426 | 60,000（完整） | 98.76% |
| 学生 MLP（独立训练） | 25,450 | 6,000（10%） | 89.51% |
| 学生 MLP（蒸馏训练） | 25,450 | 6,000（10%） | **90.51%（+1.00pp）** |

**MNIST-Fashion（学生训练数据 6000 张）：**

| 模型 | 参数量 | 训练数据量 | 测试准确率 |
|---|---|---|---|
| 教师 LeNet-5 | 44,426 | 60,000（完整） | 88.68% |
| 学生 MLP（独立训练） | 25,450 | 6,000（10%） | 81.98% |
| 学生 MLP（蒸馏训练） | 25,450 | 6,000（10%） | **82.09%（+0.11pp）** |

![MNIST 蒸馏对比](../results/exp3_distill_mnist.png)
![Fashion 蒸馏对比](../results/exp3_distill_fashion.png)

### 5.3 分析与结论

1. **蒸馏有效**：在训练数据只有 10% 的困难场景下，MNIST 上蒸馏学生比独立训练高 **1.00 个百分点**（89.51%→90.51%），说明教师 softmax 分布携带的类间结构信息确实弥补了数据不足——与 Hinton 论文"用教师软标签训练可等效于更多数据"的结论一致；
2. **提升幅度有限的原因**：统计了教师 softmax 的平均最大概率——MNIST 教师高达 **99.06%**，即教师对每个样本都"过于自信"，软化后的分布几乎退化为硬标签，能提供的"暗知识"极少；Fashion 教师为 88.71%，信息稍多但学生 MLP(32) 容量小、任务更难，提升空间被压缩；
3. **蒸馏的最佳使用场景**：本实验说明，蒸馏的收益取决于 **"教师知道但标注没告诉学生的信息量"**。在 MNIST 这类类别简单、教师置信度趋近 1 的任务上收益有限；而在 ImageNet 等 1000 类、类别高度相似的大规模任务上（教师平均置信度低、类间层级结构丰富），蒸馏提升显著。这也解释了为什么知识蒸馏最常用于**模型压缩**与**大规模分类**场景；
4. **温度的超参规律**：扫描显示 T 越低越好（T=1 最优）——高温度会过度平滑本已尖锐的教师分布，进一步稀释信息。当教师置信度低（信息丰富）时，适当升温（如 T=2~4）才有助于暴露类间相似度。

## 6. 实验四：AutoEncoder（CNN-AE vs 全连接 AE）

### 6.1 设计
在 MNIST / Fashion 上对比两种自编码器（15 epochs，MSE 重建损失）：
- **CNN-AE**：卷积编码（32→64→64→128 通道、下采样）+ 128×4×4→64 潜在向量 + 反卷积解码；
- **MLP-AE**：784→256→64→256→784。

### 6.2 结果（15 epochs，MSE 重建损失，输入 [0,1]）

| 数据集 | CNN-AE 重建 MSE | MLP-AE 重建 MSE | CNN 相对优势 |
|---|---|---|---|
| MNIST | **0.00180** | 0.00335 | 低 46% |
| MNIST-Fashion | **0.00512** | 0.00744 | 低 31% |

**重构可视化（上：原图 / 中：CNN-AE 重构 / 下：MLP-AE 重构）：**

![MNIST 重构对比](../results/exp4_recon_mnist.png)

![Fashion 重构对比](../results/exp4_recon_fashion.png)

**训练损失曲线：**

![MNIST AE 损失](../results/exp4_loss_mnist.png)
![Fashion AE 损失](../results/exp4_loss_fashion.png)

### 6.3 分析与结论

1. **CNN-AE 重建质量全面优于 MLP-AE**：两个数据集上 MSE 均降低 30%-46%，可视化可见 CNN-AE 重构的数字/服饰轮廓更清晰、细节保留更完整；
2. **CNN 结构的优势来源**：
   - 卷积的**局部感受野 + 权值共享**天然适合图像空间结构，编码器能提取有意义的局部特征（边缘、纹理）；
   - **下采样 + 反卷积**的编码-解码结构保留了空间拓扑关系，比全连接"展平-重排"更符合图像的平移不变性；
3. **MLP-AE 的劣势**：全连接把 28×28 像素展平成 784 维向量，破坏了像素间的空间邻接关系，压缩到 64 维潜在空间时会丢失大量空间细节；
4. **潜在空间的局限**：本实验验证了 AE 的重建能力，但其潜在空间无约束（不像 VAE 有正态先验），无法直接用于采样生成新样本——这正是后续 GAN 实验（实验五）要解决的问题。

### 6.4 延伸拓展：潜在空间的"背诵 vs 理解"验证（插值实验）

重建好可能有两种截然不同的原因：**死记硬背**（记住每张图怎么"抄"）与**真学到了结构**（理解了笔画规律）。前者换输入就泛化差、潜在空间是散乱的点；后者泛化好、潜在空间连续且有语义。为区分二者，本实验额外做了两个可视化验证（不在任务必做范围内，用于检验 AE 内部学到的表征质量）。

**① 潜在空间分布（PCA 降维）**

把 MNIST 测试集中 0-9 各 30 张图编码为 64 维潜在向量 z，再用 PCA 降到 2 维绘制散点图：

![潜在空间分布](../results/exp4_latent_space.png)

可以看到：**同一数字的样本聚成紧密的一团，不同数字的团互相分离**，例如数字 7 与 3 的团中心相距很远。这说明 z 按"内容语义"而非"像素位置"组织——网络确实学到了"这是什么数字"的表征，而不是把图像当字典逐张抄写。

**② 潜在空间插值（7 → 3）**

取一个"7"的 z₇ 与一个"3"的 z₃，在两者之间线性插值（t=0 → t=1），逐步解码成图：

![潜在空间插值](../results/exp4_interp.png)

插值中间帧呈现"7 的笔画逐渐形变为 3"的过渡，而非随机乱码，进一步印证 z 空间是**连续且有结构的**——这正是"表征学习"的核心价值。**同时**，中间帧仍明显模糊、不自然，说明 AE 的 z 空间缺少分布约束（任意中间点并不保证是"好看的图"），这是 AE 的直接局限，也正是 **VAE（为 z 施加正态先验）** 与 **GAN（用判别器逼真）** 的改进动机。

> 小结：插值实验不产生报告必需的数据，却是检验"AE 是理解还是背诵"的试金石，同时直观暴露了 AE 潜在空间无约束的缺陷，为后续 VAE / GAN 的学习提供了动机。

## 7. 实验五：GAN 与 CGAN

### 7.1 设计
在 MNIST 上实现 GAN 与条件 GAN（CGAN），生成器 100 维噪声 → 28×28 图像（反卷积上采样），判别器为卷积二分类网络；CGAN 将类别标签 one-hot 嵌入分别注入生成器与判别器。Adam(2e-4) 训练。

### 7.2 结果

在 MNIST 上各训练 15 epochs（Adam lr=2e-4，CPU 环境）。生成器与判别器的对抗损失曲线、最终生成样本如下：

| 模型 | 判别器最终损失 | 生成器最终损失 |
|---|---|---|
| GAN | 0.312 | 1.936 |
| CGAN | 0.728 | 2.714 |

**GAN 生成效果（随机噪声 → 数字）：**

![GAN 生成结果](../results/exp5_gan_mnist_final.png)

**CGAN 生成效果（按标签条件生成 0-9，每列对应一个数字）：**

![CGAN 生成结果](../results/exp5_cgan_mnist_final.png)

**训练曲线：**

![GAN 损失曲线](../results/exp5_gan_mnist_loss.png)
![CGAN 损失曲线](../results/exp5_cgan_mnist_loss.png)

### 7.3 分析与结论

1. **GAN 能生成可辨识的数字**：经过 15 epochs 对抗训练，生成器学会了逼近 MNIST 数据分布，生成的数字在形状、笔触上与真实手写体相似，验证了 min-max 博弈训练的有效性；
2. **CGAN 实现"按标签定向生成"**：加入类别条件后，每一列生成的数字与对应标签一致（0-9 清晰可辨），体现了条件信息对生成内容的控制力——这是从"随机生成"到"可控生成"的关键一步；
3. **CGAN 训练难度略高**（D loss 0.73 > GAN 的 0.31）：条件变量增加了判别器的输入信息，对抗收敛更慢，需要更多 epoch 才能达到与 GAN 相同的视觉质量；
4. **训练不稳定性的观察**：生成器损失（G loss）在两个模型中都出现波动而非单调下降，反映了 GAN 训练不稳定的典型特征（模式坍缩倾向、判别器-生成器能力失衡）。未来可尝试 WGAN 的 Wasserstein 距离、谱归一化等改进。

## 8. 实验六：预训练模型在 ImageNet 上的验证（完整完成）

### 8.1 结构分析（参数量 / FLOPs / 存储）

| 模型 | 参数量 | 单图 224² 推理 FLOPs | 存储大小（fp32） |
|---|---|---|---|
| AlexNet | 61.10M | 1.43 G | 244.4 MB |
| VGG-16 | 138.36M | 30.94 G | 553.4 MB |
| ResNet-50 | 25.56M | 8.18 G | 102.2 MB |

![模型结构与复杂度对比](../results/exp6_model_comparison.png)

### 8.2 ImageNet 验证结果

使用 **ImageNet-1k 官方验证集子集（每类 5 张，共 5000 张）** 测试三种预训练模型（torchvision 预训练权重），评测 Top-1 / Top-5 准确率：

| 模型 | 参数量 | Top-1 准确率 | Top-5 准确率 | 5000 张推理耗时 |
|---|---|---|---|---|
| AlexNet | 61.10M | 0.5672（56.72%） | 0.7904（79.04%） | 294 s |
| VGG-16 | 138.36M | 0.7224（72.24%） | 0.9046（90.46%） | 1788 s |
| ResNet-50 | 25.56M | **0.7732（77.32%）** | **0.9336（93.36%）** | 820 s |

![ImageNet 准确率对比](../results/exp6_imagenet_accuracy.png)

![ImageNet 预测示例（Top-5）](../results/exp6_imagenet_predictions.png)

### 8.3 分析

1. **结果与官方（论文/torchvision 文档）高度一致**：AlexNet ~56.5%、VGG-16 ~71.6%、ResNet-50 ~76.1%（官方均为完整 5 万张验证集），我们 5000 张子集的实测值与之吻合（56.7% / 72.2% / 77.3%），说明评测流程可靠；
2. **模型容量与性能正相关，但效率差异显著**：ResNet-50 参数最少（25.6M，仅为 VGG 的 1/5）、FLOPs 仅为 VGG 的 1/4，Top-1 却最高（77.3%），比 VGG 高 5 个百分点——**Bottleneck + 残差连接实现了"更少的算力、更高的精度"**；
3. **VGG-16 效率最低**：1.38 亿参数 + 31 GFLOPs，精度却不如参数更少的 ResNet-50，验证了"深而规整但不高效"的结论，也解释了它为何逐渐被 ResNet 取代；
4. **Top-1 与 Top-5 的差距**：约 16-22 个百分点，说明 ImageNet 1000 类中存在大量相似类别（如犬种），模型虽不能精确到第 1 名，但能识别到语义相近的类别——这也正是 softmax 分布携带"类间相似性"信息的来源，与实验三知识蒸馏的分析呼应；
5. 完整验证集（5 万张）可直接用 `python exp6_pretrained.py` 运行（数据已就绪于 `data/imagenet/val`）。

## 9. 选做实验（已完成）

### 9.1 Pix2Pix 风格迁移（边缘图 → 建筑立面）

**方法**：条件 GAN（Isola 2017）——UNet 生成器（含跳跃连接保留空间细节）+ PatchGAN 判别器（70×70 分块判真伪）+ 损失 = GAN 对抗损失 + L1 重建损失（λ=100）。数据集：facades（400 张边缘图/建筑立面配对图，512×256）。

**训练**：CPU 环境 8 epochs，生成器损失从 46.97 降至 37.48（L1 分量占主导，符合 Pix2Pix 特征）。

**生成效果**（每行：输入边缘图 | 模型生成 | 真实建筑立面）：

![Pix2Pix 生成效果](../results/exp7_pix2pix_ep8.png)

**分析与结论**：
1. 模型学会从边缘骨架生成较逼真的建筑立面——窗户、墙体、屋顶轮廓得到还原，L1 项保证了结构对齐，PatchGAN 项改善了纹理真实感；
2. 生成结果与真实图仍存在差异（颜色、细节不完美），因为仅 8 epochs 且 CPU 训练（论文为 200 epochs + GPU），也体现了 GAN 训练"慢热"的特点；
3. 验证了 Pix2Pix"配对数据 + 条件 GAN + 结构化输出"的通用框架思想——同一套代码稍改数据即可迁移到其他图像翻译任务。

### 9.2 YOLOv3 目标检测（COCO 80 类）

**方法**：OpenCV DNN 加载 Darknet 预训练 YOLOv3（cfg + 248MB weights），输入 416×416，NMS 去重。测试图取自 ImageNet 验证集（狗/猫/斑马/大象/卡车等）。

**检测结果**（置信度阈值 0.4）：

| 测试图 | 检测结果（类别 + 置信度） |
|---|---|
| 柯基犬图 | dog(1.00) ✅ |
| 波斯猫图 | cat(0.90) ✅ |
| 牧羊犬图 | dog(0.99) ✅ |
| 斑点犬图 | dog(0.99), person(0.98) ✅ |
| 斑马图 | zebra(0.99) ✅ |
| 卡车图 | truck(0.99), truck(0.80) ✅ |
| 大象图 | elephant(0.67) + 6×bird |
| 红熊猫图 | bear(0.65)（视觉相似误检） |

![YOLOv3 检测效果](../results/exp8_yolov3_results.png)

**分析与结论**：
1. **准确率**：对主流目标（狗、猫、斑马、卡车、大象）都能以高置信度正确识别，多目标图（大象与鸟群、犬与人）也能同时检出；
2. **速度**：CPU 下单张 416×416 推理仅 1-1.5 秒，远超 two-stage 方法，体现了 YOLO"一次前向回归"的实时性优势；
3. **误检分析**：红熊猫（红棕毛色、体型相似）被识别为 bear(0.65)，大象图中检出 bird——单阶段模型对相似外观/小目标仍存在混淆，置信度也相对低，这正是 one-stage 在细粒度与小目标上的固有短板；
4. 整体验证了 YOLOv3 多尺度预测（FPN 式 3 个尺度）与锚框机制的工程有效性。

## 10. 实验总结与学习心得

### 10.1 实验成果一览

| 实验 | 核心发现 |
|---|---|
| 实验一 LeNet-5 vs 全连接 | CNN 以更少参数（CIFAR 上少 27.5 倍）全面胜过 MLP，收敛后 CIFAR 仍高 17.3pp |
| 实验二 超参数对比 | 大核/大通道/max 池化/ReLU 更优；**sigmoid 因梯度消失几乎不收敛（11.35%）** |
| 实验三 知识蒸馏 | 10% 数据场景下，蒸馏使 MNIST 学生提升 +1.00pp；收益取决于教师"暗知识"量 |
| 实验四 自编码器 | CNN-AE 重建 MSE 比 MLP-AE 低 30%-46%，卷积结构更契合图像重建 |
| 实验五 GAN/CGAN | GAN 可生成逼真数字；CGAN 实现按标签定向生成 |
| 实验六 预训练模型 | 实测 ImageNet top-1：AlexNet 56.7% < VGG-16 72.2% < ResNet-50 77.3%，后者参数最少、算力最省、精度最高 |
| 选做① Pix2Pix | 边缘图成功翻译为建筑立面，L1 保结构 + PatchGAN 提纹理 |
| 选做② YOLOv3 | 单张 CPU 推理约 1s，狗/猫/斑马/卡车等准确检测，红熊猫误检为 bear 体现 one-stage 短板 |

### 10.2 学习心得

1. **从"能跑"到"懂原理"**：亲手推导卷积层梯度（`∂L/∂W=δ*a`、`∂L/∂b=Σδ`、`∂L/∂a=δ*rot180(W)`）并观察 sigmoid 的梯度消失现象，比单纯看公式理解深刻得多——实验二里 sigmoid 的 11.35% 准确率就是梯度消失最直观的教科书证据；

2. **调试是学习的核心环节**：实验三蒸馏最初"不升反降"，排查后发现两个关键点——① 软标签与训练样本顺序未对齐（shuffle 导致错位）；② 温度与权重超参不匹配任务特性。逐一修复后蒸馏终于产生正向提升。这个过程让我体会到：**实验结果不符合预期时，先检查数据对齐和实现细节，再怀疑模型本身**；

3. **CNN 系列模型的脉络**：从 LeNet-5（局部连接+权值共享）到 AlexNet（ReLU/Dropout 工程化）、VGG（小核堆叠证明深度）、ResNet（残差解决退化），再到检测领域 R-CNN→Fast→Faster→FPN 与 YOLO/SSD 两条路线的演进，我理解了**架构设计总是在"精度-速度-参数量"三者的权衡中推进**；

4. **生成式模型的谱系**：AE（表征）→ VAE（分布先验）→ GAN（对抗博弈）→ CGAN（条件控制）→ Pix2Pix（配对翻译）→ CycleGAN（无配对翻译），每步解决前一步的一个缺陷，这种"以问题为导向"的演进思路值得学习；

5. **工程现实**：在 CPU 环境下，训练策略（epoch、batch、线程数）需要为硬件量体裁衣；数据集下载、镜像选择等环境工程同样是实验成败的一部分。
